"use client";

// import createCheckoutSession from "/stripe/createCheckoutSession";

export default function Card() {
  return (
    <button
      onClick={() => {
        alert(0);
      }}
    >
      click me
    </button>
  );
}
